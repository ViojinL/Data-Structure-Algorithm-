# 词法分析器设计与实现实验报告# 词法分析器设计与实现实验报告## 实验报告：基于DFA的词法分析器设计与实现



## 一、实验目的



本实验旨在通过设计和实现一个词法分析器，加深对编译原理中词法分析基本理论的理解。具体目标包括：掌握正规式到确定有限自动机（DFA）的转化方法；理解词法分析器的工作原理和实现技术；通过实际编程体会词法分析在编译过程中的作用；培养运用数据结构和算法解决实际问题的能力。通过本次实验，能够深刻认识到词法分析作为编译器前端的重要组成部分，在源程序处理中的基础性作用。## 一、实验目的### 一、实验目的



## 二、实验内容及要求



本实验要求编制一个能够分析给定范围内的整数、标识符的词法分析器。识别对象的正规式定义如下：整数的正规式为 `0 | (1|2|3)(0|1|2|3)*`，即单独的0或以1、2、3开头后跟任意多个0到3的数字；标识符的正规式为 `<字母>(<字母>|<数字字符>)*`，其中字母限定为a、b、c，数字字符限定为0、1、2、3。本实验旨在通过设计和实现一个词法分析器，加深对编译原理中词法分析基本理论的理解。具体目标包括：掌握正规式到确定有限自动机（DFA）的转化方法；理解词法分析器的工作原理和实现技术；通过实际编程体会词法分析在编译过程中的作用；培养运用数据结构和算法解决实际问题的能力。通过本次实验，能够深刻认识到词法分析作为编译器前端的重要组成部分，在源程序处理中的基础性作用。本实验旨在通过设计和实现一个词法分析器，加深对编译原理中词法分析基本理论的理解。具体目标包括：掌握正规式到确定有限自动机（DFA）的转化方法；理解词法分析器的工作原理和实现技术；通过实际编程体会词法分析在编译过程中的作用；培养运用数据结构和算法解决实际问题的能力。



实验的核心任务包括三个部分。首先需要根据正规式画出能够识别上述整数和标识符的DFA状态转换图；其次要根据DFA状态转换图设计词法分析函数，该函数需要能够从键盘读入单个词或多个词（空格隔开）进行词法分析，并返回词的种别（整数用1表示、标识符用2表示、非法词用3表示）以及词的属性值，输出格式为种别和属性值的二元组；最后需要编写测试程序，调用词法分析函数完成四组测试：输入学号后5位数、输入姓氏首字母连接学号后2位数、输入包含至少一个标识符和一个整数的多个词、输入包含至少一个非法词的多个词。



## 三、实验过程## 二、实验内容及要求### 二、实验内容及要求



### 3.1 DFA状态转换图设计



根据给定的正规式，我设计了一个包含5个状态的确定有限自动机。初始状态为START，从该状态出发，根据读入字符的不同转移到不同的后继状态。当读入字符'0'时，转移到INT_ZERO状态，该状态是一个接受状态，表示识别到单独的整数0；当读入字符'1'、'2'或'3'时，转移到INT_NZ状态，该状态也是接受状态，表示识别到非零开头的整数；当读入字符'a'、'b'或'c'时，转移到ID状态，该状态是接受状态，表示识别到标识符的开始；对于其他任何字符，转移到DEAD状态，这是一个非接受状态，表示遇到了非法字符。本实验要求编制一个能够分析给定范围内的整数、标识符的词法分析器。识别对象的正规式定义如下：整数的正规式为 `0 | (1|2|3)(0|1|2|3)*`，即单独的0或以1、2、3开头后跟任意多个0到3的数字；标识符的正规式为 `<字母>(<字母>|<数字字符>)*`，其中字母限定为a、b、c，数字字符限定为0、1、2、3。本实验要求编制一个能够分析给定范围内的整数、标识符的词法分析器。识别对象的正规式定义如下：整数的正规式为 0 | (1|2|3)(0|1|2|3)*，即单独的0或以1、2、3开头后跟任意多个0到3的数字；标识符的正规式为 <字母>(<字母>|<数字字符>)*，其中字母限定为a、b、c，数字字符限定为0、1、2、3。



在INT_ZERO状态下，这是一个特殊的终态，表示单独的'0'是合法整数，但如果后续还有任何字符，则必须转移到DEAD状态，因为正规式不允许0后面再有其他数字。在INT_NZ状态下，如果继续读入'0'到'3'的数字，状态保持在INT_NZ，形成自循环，这样就可以识别任意长度的整数序列；如果读入其他字符，则转移到DEAD状态。在ID状态下，如果继续读入'a'到'c'的字母或'0'到'3'的数字，状态保持在ID，形成自循环，这样就可以识别任意长度的标识符；如果读入其他字符，则转移到DEAD状态。DEAD状态是一个吸收态，一旦进入该状态，无论后续读入什么字符都保持在该状态。



状态转换图的文字描述如下：实验的核心任务包括三个部分：首先需要根据正规式画出能够识别上述整数和标识符的DFA状态转换图；其次要根据DFA状态转换图设计词法分析函数，该函数需要能够从键盘读入单个词或多个词（空格隔开）进行词法分析，并返回词的种别（整数用1表示、标识符用2表示、非法词用3表示）以及词的属性值，输出格式为 `<种别, 属性值>`；最后需要编写测试程序，调用词法分析函数完成四组测试：输入学号后5位数、输入姓氏首字母连接学号后2位数、输入包含至少一个标识符和一个整数的多个词、输入包含至少一个非法词的多个词。实验的核心任务包括三个部分：首先需要根据正规式画出能够识别上述整数和标识符的DFA状态转换图；其次要根据DFA状态转换图设计词法分析函数，该函数需要能够从键盘读入单个词或多个词（空格隔开）进行词法分析，并返回词的种别（整数用1表示、标识符用2表示、非法词用3表示）以及词的属性值；最后需要编写测试程序，调用词法分析函数完成四组测试：输入学号后5位数、输入姓氏首字母连接学号后2位数、输入包含至少一个标识符和一个整数的多个词、输入包含至少一个非法词的多个词。



- START状态的转移规则：输入'0'转移到INT_ZERO（终态），输入'1'、'2'、'3'转移到INT_NZ（终态），输入'a'、'b'、'c'转移到ID（终态），输入其他字符转移到DEAD（非终态）

- INT_ZERO状态（终态，表示整数0）的转移规则：输入任何字符都转移到DEAD

- INT_NZ状态（终态，表示非零整数）的转移规则：输入'0'、'1'、'2'、'3'保持在INT_NZ状态，输入其他字符转移到DEAD## 三、实验过程

- ID状态（终态，表示标识符）的转移规则：输入'a'、'b'、'c'、'0'、'1'、'2'、'3'保持在ID状态，输入其他字符转移到DEAD

- DEAD状态（非终态，表示非法）的转移规则：输入任何字符都保持在DEAD状态



### 3.2 数据结构设计### 3.1 DFA状态转换图设计



在实现词法分析器时，我采用了C++的标准模板库提供的多种数据结构。首先，使用枚举类型TokenType定义了三种词法单元类型：INTEGER等于1表示整数、IDENTIFIER等于2表示标识符、ILLEGAL等于3表示非法词。这种设计使得程序中对词法单元类型的处理更加清晰和类型安全。ASCII 示意图如下（可用此图示意，正式报告中建议插入你用 Word/手绘的状态图截图）：



其次，使用`pair<int, string>`作为词法分析函数的返回类型，其中第一个元素是整型的种别码，第二个元素是字符串类型的属性值（即原始的词素）。这种设计符合实验要求中输出格式，使得返回值的语义非常明确。根据给定的正规式，我设计了一个包含5个状态的确定有限自动机。初始状态为START，从该状态出发，根据读入字符的不同转移到不同的后继状态。当读入字符'0'时，转移到INT_ZERO状态，该状态是一个接受状态，表示识别到单独的整数0；当读入字符'1'、'2'或'3'时，转移到INT_NZ状态，该状态也是接受状态，表示识别到非零开头的整数；当读入字符'a'、'b'或'c'时，转移到ID状态，该状态是接受状态，表示识别到标识符的开始；对于其他任何字符，转移到DEAD状态，这是一个非接受状态，表示遇到了非法字符。



再次，使用`vector<pair<int, string>>`存储对一行输入中多个词的分析结果。选择vector作为容器是因为需要按照输入顺序依次输出每个词的分析结果，vector能够保持元素的插入顺序，并且支持高效的尾部插入操作。```



最后，使用stringstream进行字符串的分词处理。stringstream是C++标准库提供的字符串流类，它可以像操作文件流一样操作字符串，使用提取运算符可以自动按照空白字符（空格、制表符、换行符等）分割字符串，这大大简化了多词输入的处理逻辑。在INT_ZERO状态下，这是一个特殊的终态，表示单独的'0'是合法整数，但如果后续还有任何字符，则必须转移到DEAD状态，因为正规式不允许0后面再有其他数字。在INT_NZ状态下，如果继续读入'0'到'3'的数字，状态保持在INT_NZ，形成自循环，这样就可以识别任意长度的整数序列；如果读入其他字符，则转移到DEAD状态。在ID状态下，如果继续读入'a'到'c'的字母或'0'到'3'的数字，状态保持在ID，形成自循环，这样就可以识别任意长度的标识符；如果读入其他字符，则转移到DEAD状态。DEAD状态是一个吸收态，一旦进入该状态，无论后续读入什么字符都保持在该状态。            +----------------------+



在DFA的实现中，使用枚举类型State定义了五个状态：START（初始状态）、INT_ZERO（识别单独的0）、INT_NZ（识别非零整数）、ID（识别标识符）、DEAD（非法状态）。使用枚举而不是整数常量使得代码的可读性更强，状态转移逻辑更加清晰。            |         START        |



### 3.3 核心函数设计与实现状态转换图的文字描述如下：            +----------------------+



#### 3.3.1 辅助判断函数```             | '0'      | '1'-'3'   | 'a'-'c'   | other



为了提高代码的可读性和复用性，我设计了三个内联辅助函数用于字符判断。`isDigit03`函数用于判断字符是否为'0'到'3'的数字，实现代码为 `return c >= '0' && c <= '3'`，通过比较字符的ASCII码值来判断；`isDigit123`函数用于判断字符是否为'1'到'3'的数字，实现代码为 `return c >= '1' && c <= '3'`，这个函数专门用于判断整数的首字符；`isLetterABC`函数用于判断字符是否为小写字母'a'、'b'或'c'，实现代码为 `return c >= 'a' && c <= 'c'`。这三个函数都声明为static inline，其中static限制函数的作用域在当前编译单元，inline建议编译器进行内联优化以提高性能。START状态：             v          v           v           v



#### 3.3.2 词法分析核心函数  - 输入'0' → INT_ZERO（终态）         +-------+  +----------+  +------+    +------+



lex_analyzer函数是整个程序的核心，它接受一个字符串参数lexeme表示待分析的词素，返回一个pair表示该词的种别和属性值。函数首先检查输入是否为空字符串，如果是则直接返回`{ILLEGAL, lexeme}`，因为空字符串不是合法的词法单元。  - 输入'1','2','3' → INT_NZ（终态）         |INT_0 |  |  INT_NZ  |  |  ID  |    | DEAD |



然后定义一个状态变量s并初始化为START状态，这是DFA的初始状态。接下来使用for循环遍历输入字符串的每个字符，在循环体内使用switch语句根据当前状态和输入字符决定状态转移。  - 输入'a','b','c' → ID（终态）         +-------+  +----------+  +------+    +------+



在START状态下，如果读到字符'0'，则转移到INT_ZERO状态，这对应于识别单独的整数0；如果读到字符'1'、'2'或'3'（通过调用isDigit123函数判断），则转移到INT_NZ状态，开始识别非零整数；如果读到字母'a'、'b'或'c'（通过调用isLetterABC函数判断），则转移到ID状态，开始识别标识符；对于其他任何字符，都转移到DEAD状态。  - 输入其他字符 → DEAD（非终态）             |           |  ^        |  ^



在INT_ZERO状态下，这是一个关键的设计点。由于正规式规定整数只能是单独的0或者以非零数字开头的序列，因此单独的'0'后面不能再有任何字符。所以在这个状态下，无论读到什么字符，都直接转移到DEAD状态。这个设计确保了"01"、"02"这样的输入会被正确识别为非法。具体代码如下：           other        0-3 |      a-c|0-3



```cppINT_ZERO状态（终态，表示整数0）：             v              |         | 

case INT_ZERO:

    // 单独的0后面不能有其他字符  - 输入任何字符 → DEAD           +------+         +---------+ 

    s = DEAD;

    break;           | DEAD |                    

```

INT_NZ状态（终态，表示非零整数）：           +------+

在INT_NZ状态下，如果继续读到'0'到'3'的数字（通过调用isDigit03函数判断），状态保持在INT_NZ，这样可以识别任意长度的整数如"123"、"3210"等；如果读到其他字符如'4'或字母，则转移到DEAD状态。

  - 输入'0','1','2','3' → INT_NZ（保持）

在ID状态下，如果继续读到字母'a'、'b'、'c'或数字'0'到'3'（通过调用isLetterABC和isDigit03函数判断），状态保持在ID，这样可以识别任意长度的标识符如"abc"、"a12b3"等；如果读到其他字符如字母'd'或数字'4'，则转移到DEAD状态。

  - 输入其他字符 → DEAD终态：INT_0、INT_NZ、ID；非法吸收态：DEAD

在每次状态转移后，代码立即检查当前状态是否为DEAD，如果是则提前结束循环并返回`{ILLEGAL, lexeme}`。这是一个性能优化，避免了在已经确定为非法词的情况下继续处理剩余字符。代码如下：

```

```cpp

if (s == DEAD) {ID状态（终态，表示标识符）：

    return {ILLEGAL, lexeme};

}  - 输入'a','b','c','0','1','2','3' → ID（保持）#### 3.2 算法流程（详细）

```

  - 输入其他字符 → DEAD- 输入一个词 lexeme；

遍历完所有字符后，根据最终状态判断词的类型。如果最终状态是INT_ZERO或INT_NZ，返回`{INTEGER, lexeme}`表示识别到整数；如果最终状态是ID，返回`{IDENTIFIER, lexeme}`表示识别到标识符；否则返回`{ILLEGAL, lexeme}`表示非法词。

- 从 START 状态开始：

#### 3.3.3 多词处理函数

DEAD状态（非终态，表示非法）：  - 读首字符：

process_input函数用于处理包含多个词的输入行。该函数接受一个字符串参数line表示一行输入，返回一个`vector<pair<int, string>>`包含所有词的分析结果。函数内部首先创建一个空的results向量用于存储结果，然后创建一个stringstream对象ss并用输入行初始化。

  - 输入任何字符 → DEAD（保持）    - '0' → INT_0；

接下来使用while循环和提取运算符从字符串流中逐个读取词素。stringstream的提取运算符会自动跳过前导空白字符，并在遇到空白字符时停止读取，这样就自然地实现了按空格分词的功能。对于读取到的每个词素tok，调用lex_analyzer函数进行词法分析，并将返回的结果添加到results向量中。循环会一直进行到字符串流中没有更多词素为止。核心代码如下：

```    - '1'..'3' → INT_NZ；

```cpp

while (ss >> tok) {    - 'a'..'c' → ID；

    results.push_back(lex_analyzer(tok));

}### 3.2 数据结构设计    - 其他 → DEAD；

```

  - 继续读后续字符：

这个函数的设计使得程序可以优雅地处理单词输入和多词输入两种情况。对于单词输入，循环只执行一次；对于多词输入，循环会对每个词分别进行分析。同时，这个设计还自动处理了输入中可能存在的多余空格问题，无论词与词之间有一个空格还是多个空格，都能正确分词。

在实现词法分析器时，我采用了C++的标准模板库提供的多种数据结构。首先，使用枚举类型 `TokenType` 定义了三种词法单元类型：`INTEGER = 1` 表示整数、`IDENTIFIER = 2` 表示标识符、`ILLEGAL = 3` 表示非法词。这种设计使得程序中对词法单元类型的处理更加清晰和类型安全。    - 若处于 INT_0：出现任意后续字符即 DEAD（仅允许单独“0”）。

#### 3.3.4 结果输出函数

    - 若处于 INT_NZ：仅允许 '0'..'3' 循环保持 INT_NZ，否则 DEAD。

print_results函数用于打印词法分析的结果。该函数接受一个`vector<pair<int, string>>`参数，使用范围for循环遍历向量中的每个元素，按照`<种别, 属性值>`的格式输出。具体代码为：

其次，使用 `pair<int, string>` 作为词法分析函数的返回类型，其中第一个元素是整型的种别码，第二个元素是字符串类型的属性值（即原始的词素）。这种设计符合实验要求中 `<种别, 属性值>` 的输出格式，使得返回值的语义非常明确。    - 若处于 ID：仅允许 'a'..'c' 或 '0'..'3' 循环保持 ID，否则 DEAD。

```cpp

for (const auto &p : res) {- 读完：

    cout << "<" << p.first << ", " << p.second << ">" << endl;

}再次，使用 `vector<pair<int, string>>` 存储对一行输入中多个词的分析结果。选择vector作为容器是因为需要按照输入顺序依次输出每个词的分析结果，vector能够保持元素的插入顺序，并且支持高效的尾部插入操作。  - INT_0 或 INT_NZ → 整数(1)；

```

  - ID → 标识符(2)；

其中p.first是种别码，p.second是属性值。这个输出格式完全符合实验要求。

最后，使用 `stringstream` 进行字符串的分词处理。stringstream是C++标准库提供的字符串流类，它可以像操作文件流一样操作字符串，使用提取运算符 `>>` 可以自动按照空白字符（空格、制表符、换行符等）分割字符串，这大大简化了多词输入的处理逻辑。  - 其他或 DEAD → 非法(3)。

#### 3.3.5 测试驱动函数



test函数实现了实验要求的四组测试。每组测试都遵循相同的模式：首先打印测试提示信息，然后使用getline函数从标准输入读取一行，接着调用process_input函数对输入进行词法分析，最后调用print_results函数输出结果。

在DFA的实现中，使用枚举类型 `State` 定义了五个状态：`START`（初始状态）、`INT_ZERO`（识别单独的0）、`INT_NZ`（识别非零整数）、`ID`（识别标识符）、`DEAD`（非法状态）。使用枚举而不是整数常量使得代码的可读性更强，状态转移逻辑更加清晰。边界与特判：空串直接非法；大小写敏感，仅允许小写 a..c；数字仅限 0..3。

测试1要求输入学号后5位数。由于数字范围限定在0到3，如果学号包含4到9的数字，将被识别为非法。测试2要求输入姓氏首字母连接学号后2位数。如果姓氏首字母在a到c范围内且学号数字在0到3范围内，将被识别为标识符；否则为非法。测试3要求输入多个词，其中至少包含一个标识符和一个整数，这个测试验证了程序处理多词输入的能力。测试4要求输入至少包含一个非法词，验证了程序的错误检测能力。



所有测试统一使用getline而不是`cin >>`，这样可以避免输入缓冲区残留换行符的问题，使得四组测试能够顺利地依次执行。

### 3.3 核心函数设计与实现#### 3.3 数据结构与函数设计

#### 3.3.6 主函数

- 数据结构

main函数是程序的入口。函数首先调用`ios::sync_with_stdio(false)`关闭C++标准流与C标准流的同步，这可以提高输入输出的性能。然后调用`cin.tie(nullptr)`解除cin和cout的绑定，进一步提高性能。虽然对于这个小程序来说性能提升不明显，但这是一个良好的编程习惯。最后调用test函数执行测试，程序返回0表示正常结束。

#### 3.3.1 辅助判断函数  - string：保存词法单元；

### 3.4 完整源代码

  - enum TokenType：1-整数、2-标识符、3-非法；

```cpp

#include <iostream>为了提高代码的可读性和复用性，我设计了三个内联辅助函数用于字符判断。`isDigit03(char c)` 函数用于判断字符是否为0到3的数字，实现代码为 `return c >= '0' && c <= '3'`，通过比较字符的ASCII码值来判断；`isDigit123(char c)` 函数用于判断字符是否为1到3的数字，实现代码为 `return c >= '1' && c <= '3'`，这个函数专门用于判断整数的首字符；`isLetterABC(char c)` 函数用于判断字符是否为小写字母a、b或c，实现代码为 `return c >= 'a' && c <= 'c'`。这三个函数都声明为 `static inline`，其中static限制函数的作用域在当前编译单元，inline建议编译器进行内联优化以提高性能。  - pair<int,string>：返回 <种别, 属性>。

#include <vector>

#include <string>- 函数说明（C++ 实现）

#include <sstream>

#### 3.3.2 词法分析核心函数  - lex_analyzer(const string& lexeme)

using namespace std;

    - 输入：单个词 lexeme；输出：pair<int,string>；

// 定义词法单元类型：1-整数，2-标识符，3-非法

enum TokenType { INTEGER = 1, IDENTIFIER = 2, ILLEGAL = 3 };`lex_analyzer` 函数是整个程序的核心，它接受一个字符串参数 `lexeme` 表示待分析的词素，返回一个 `pair<int, string>` 表示该词的种别和属性值。函数首先检查输入是否为空字符串，如果是则直接返回 `{ILLEGAL, lexeme}`，因为空字符串不是合法的词法单元。    - 功能：按 DFA 逐字符转移并判定终态；



// 判断字符是否为0到3的数字    - 失败模式：包含非法字符或不满足正规式时返回 3（非法）。

static inline bool isDigit03(char c) { return c >= '0' && c <= '3'; }

// 判断字符是否为1到3的数字（用于整数首字符）然后定义一个状态变量 `s` 并初始化为 `START` 状态，这是DFA的初始状态。接下来使用for循环遍历输入字符串的每个字符，在循环体内使用switch语句根据当前状态和输入字符决定状态转移。  - process_input(const string& line)

static inline bool isDigit123(char c) { return c >= '1' && c <= '3'; }

// 判断字符是否为a到c的字母    - 输入：一行文本（空格分隔多个词）；输出：vector<pair<int,string>>；

static inline bool isLetterABC(char c) { return c >= 'a' && c <= 'c'; }

在START状态下，如果读到字符'0'，则转移到INT_ZERO状态，这对应于识别单独的整数0；如果读到字符'1'、'2'或'3'（通过调用isDigit123函数判断），则转移到INT_NZ状态，开始识别非零整数；如果读到字母'a'、'b'或'c'（通过调用isLetterABC函数判断），则转移到ID状态，开始识别标识符；对于其他任何字符，都转移到DEAD状态。    - 功能：分词后逐个调用 lex_analyzer。

// 词法分析核心函数

// 输入：待分析的词素（字符串）  - print_results(...)

// 输出：pair<种别码, 属性值>

pair<int, string> lex_analyzer(const string &lexeme) {在INT_ZERO状态下，这是一个关键的设计点。由于正规式规定整数只能是单独的0或者以非零数字开头的序列，因此单独的'0'后面不能再有任何字符。所以在这个状态下，无论读到什么字符，都直接转移到DEAD状态。这个设计确保了"01"、"02"这样的输入会被正确识别为非法。    - 功能：将结果按“<种别, 属性值>”打印。

    if (lexeme.empty()) return {ILLEGAL, lexeme};

  - test()

    // 定义DFA的五个状态

    enum State { START, INT_ZERO, INT_NZ, ID, DEAD };在INT_NZ状态下，如果继续读到'0'到'3'的数字（通过调用isDigit03函数判断），状态保持在INT_NZ，这样可以识别任意长度的整数如"123"、"3210"等；如果读到其他字符如'4'或字母，则转移到DEAD状态。    - 功能：完成四组交互式测试。

    State s = START;



    // 逐字符扫描，进行状态转移

    for (size_t i = 0; i < lexeme.size(); ++i) {在ID状态下，如果继续读到字母'a'、'b'、'c'或数字'0'到'3'（通过调用isLetterABC和isDigit03函数判断），状态保持在ID，这样可以识别任意长度的标识符如"abc"、"a12b3"等；如果读到其他字符如字母'd'或数字'4'，则转移到DEAD状态。（可选）函数契约小结：

        char c = lexeme[i];

        switch (s) {- 输入：非空或空字符串；

            case START:

                if (c == '0') s = INT_ZERO;                       在每次状态转移后，代码立即检查当前状态是否为DEAD，如果是则提前结束循环并返回 `{ILLEGAL, lexeme}`。这是一个性能优化，避免了在已经确定为非法词的情况下继续处理剩余字符。- 输出：枚举种别与原词；

                else if (isDigit123(c)) s = INT_NZ;                

                else if (isLetterABC(c)) s = ID;                  - 成功：返回 1 或 2；

                else s = DEAD;

                break;遍历完所有字符后，根据最终状态判断词的类型。如果最终状态是INT_ZERO或INT_NZ，返回 `{INTEGER, lexeme}` 表示识别到整数；如果最终状态是ID，返回 `{IDENTIFIER, lexeme}` 表示识别到标识符；否则返回 `{ILLEGAL, lexeme}` 表示非法词。- 失败：返回 3；

            case INT_ZERO:

                // 单独的0后面不能有其他字符- 复杂度：O(n) 单词长度线性。

                s = DEAD;

                break;#### 3.3.3 多词处理函数

            case INT_NZ:

                if (isDigit03(c)) s = INT_NZ;                    #### 3.4 测试设计与过程

                else s = DEAD;

                break;`process_input` 函数用于处理包含多个词的输入行。该函数接受一个字符串参数 `line` 表示一行输入，返回一个 `vector<pair<int, string>>` 包含所有词的分析结果。函数内部首先创建一个空的results向量用于存储结果，然后创建一个stringstream对象ss并用输入行初始化。- 测试1：输入学号后5位（仅数字 0..3 组合方为合法整数；否则非法）。

            case ID:

                if (isLetterABC(c) || isDigit03(c)) s = ID;       - 测试2：输入“姓氏首字母 + 学号后2位”（若字母在 a..c 且数字在 0..3，则标识符；否则非法）。

                else s = DEAD;

                break;接下来使用while循环和提取运算符 `>>` 从字符串流中逐个读取词素。stringstream的提取运算符会自动跳过前导空白字符，并在遇到空白字符时停止读取，这样就自然地实现了按空格分词的功能。对于读取到的每个词素tok，调用lex_analyzer函数进行词法分析，并将返回的结果添加到results向量中。循环会一直进行到字符串流中没有更多词素为止。- 测试3：输入多个词（至少含一个标识符、一个整数），逐个输出种别与属性值。

            case DEAD:

                // 保持在DEAD状态- 测试4：输入多个词（至少包含一个非法词），验证程序能正确识别非法。

                break;

        }这个函数的设计使得程序可以优雅地处理单词输入和多词输入两种情况。对于单词输入，循环只执行一次；对于多词输入，循环会对每个词分别进行分析。同时，这个设计还自动处理了输入中可能存在的多余空格问题，无论词与词之间有一个空格还是多个空格，都能正确分词。



        // 优化：一旦进入DEAD状态立即返回建议：将四组运行界面截图（终端输入与输出）插入本节，命名为“图1~图4”。截图可带水印（学号/姓名）。

        if (s == DEAD) {

            return {ILLEGAL, lexeme};#### 3.3.4 结果输出函数

        }

    }示例（节选）：



    // 根据终态判断词的类型`print_results` 函数用于打印词法分析的结果。该函数接受一个 `vector<pair<int, string>>` 参数，使用范围for循环遍历向量中的每个元素，按照 `<种别, 属性值>` 的格式输出。具体代码为 `cout << "<" << p.first << ", " << p.second << ">" << endl`，其中p.first是种别码，p.second是属性值。这个输出格式完全符合实验要求。```

    if (s == INT_ZERO || s == INT_NZ) return {INTEGER, lexeme};

    if (s == ID) return {IDENTIFIER, lexeme};输入：0 a2 23 ab 10

    return {ILLEGAL, lexeme};

}#### 3.3.5 测试驱动函数输出：



// 处理一行输入（可能包含多个词，空格分隔）<1, 0>

// 输入：一行字符串

// 输出：所有词的分析结果`test` 函数实现了实验要求的四组测试。每组测试都遵循相同的模式：首先打印测试提示信息，然后使用getline函数从标准输入读取一行，接着调用process_input函数对输入进行词法分析，最后调用print_results函数输出结果。<2, a2>

vector<pair<int, string>> process_input(const string &line) {

    vector<pair<int, string>> results;<1, 23>

    stringstream ss(line);

    string tok;测试1要求输入学号后5位数。由于数字范围限定在0到3，如果学号包含4到9的数字，将被识别为非法。测试2要求输入姓氏首字母连接学号后2位数。如果姓氏首字母在a到c范围内且学号数字在0到3范围内，将被识别为标识符；否则为非法。测试3要求输入多个词，其中至少包含一个标识符和一个整数，这个测试验证了程序处理多词输入的能力。测试4要求输入至少包含一个非法词，验证了程序的错误检测能力。<2, ab>

    while (ss >> tok) {

        results.push_back(lex_analyzer(tok));<1, 10>

    }

    return results;所有测试统一使用getline而不是cin>>，这样可以避免输入缓冲区残留换行符的问题，使得四组测试能够顺利地依次执行。```

}



// 打印分析结果

void print_results(const vector<pair<int, string>> &res) {#### 3.3.6 主函数#### 3.5 实验过程记录（出错与修复）

    for (const auto &p : res) {

        cout << "<" << p.first << ", " << p.second << ">" << endl;- 错误1：将“0”视为通用前导位，导致“01”被当作整数。

    }

}`main` 函数是程序的入口。函数首先调用 `ios::sync_with_stdio(false)` 关闭C++标准流与C标准流的同步，这可以提高输入输出的性能。然后调用 `cin.tie(nullptr)` 解除cin和cout的绑定，进一步提高性能。虽然对于这个小程序来说性能提升不明显，但这是一个良好的编程习惯。最后调用test函数执行测试，程序返回0表示正常结束。  - 严重程度：中；解决：分离 INT_0 与 INT_NZ 状态，INT_0 后续直接非法。



// 测试函数：按要求完成四组测试- 错误2：混用 `cin >>` 与 `getline` 导致读入残留换行。

void test() {

    // 测试1：学号后5位### 3.4 完整源代码  - 严重程度：低；解决：统一使用 `getline`，并关闭同步加速 IO。

    cout << "=== 测试1：学号后5位 ===" << endl;

    cout << "请输入学号后5位：";- 错误3：多空格分词异常处理。

    string input1; getline(cin, input1);

    print_results(process_input(input1));```cpp  - 严重程度：低；解决：使用 `stringstream`，自动跳过多余空白。



    // 测试2：姓氏首字母+学号后2位#include <iostream>

    cout << "\n=== 测试2：姓氏首字母+学号后2位 ===" << endl;

    cout << "请输入姓氏首字母+学号后2位：";#include <vector>#### 3.6 差异化改动（可选）

    string input2; getline(cin, input2);

    print_results(process_input(input2));#include <string>- A. 输入输出：可增加从文件读取（input.txt）并将结果写入（output.txt）。



    // 测试3：多个词（含标识符和整数）#include <sstream>- B. 数据结构：可改为“状态×字符类”的转移表，便于扩展字符集与关键字。

    cout << "\n=== 测试3：多个词（含标识符和整数） ===" << endl;

    cout << "请输入多个词（空格分隔）：";- C. 主界面：启动时打印课程/学号/姓名，并在输出中增加水印。

    string input3; getline(cin, input3);

    print_results(process_input(input3));using namespace std;- D. 文法扩展：放宽到 a..z、0..9；或加入分隔符、运算符等词法单元。



    // 测试4：多个词（含非法词）

    cout << "\n=== 测试4：多个词（含非法词） ===" << endl;

    cout << "请输入多个词（空格分隔，含非法词）：";// 定义词法单元类型：1-整数，2-标识符，3-非法### 四. 实验总结（心得）

    string input4; getline(cin, input4);

    print_results(process_input(input4));enum TokenType { INTEGER = 1, IDENTIFIER = 2, ILLEGAL = 3 };本次实验从正规式到 DFA 的构建，再到代码中显式状态机的实现，让我对词法分析器的核心机制有了直观认识。将“0”与“非零起始”分成两个状态，有效避免了前导零引发的歧义；用 `stringstream` 处理多词输入，既简洁又稳健。在测试中，我针对边界用例（空串、越界字符、大小写）逐步完善逻辑与提示，使得输出与题目格式严格一致。若要进一步优化，可改为表驱动的 DFA，便于扩展字符类与关键字；也可以提供文件 I/O 与更友好的主界面。总体而言，程序结构清晰，时间复杂度线性，具备较好的可读性与可维护性。

}



int main() {

    ios::sync_with_stdio(false);// 判断字符是否为0到3的数字---

    cin.tie(nullptr);

    test();static inline bool isDigit03(char c) { return c >= '0' && c <= '3'; }

    return 0;

}// 判断字符是否为1到3的数字（用于整数首字符）## 附录一：C++ 源代码（与提交代码一致）

```

static inline bool isDigit123(char c) { return c >= '1' && c <= '3'; }

### 3.5 程序测试过程

// 判断字符是否为a到c的字母```cpp

测试按照实验要求进行了四组，每组测试都经过精心设计以验证程序的不同功能。以下是测试的详细过程和结果分析。

static inline bool isLetterABC(char c) { return c >= 'a' && c <= 'c'; }#include <iostream>

测试1针对学号后5位进行。假设输入学号后5位为"12030"，分析过程如下：程序首先读取字符'1'，从START状态转移到INT_NZ状态；继续读取'2'，由于'2'在0到3范围内，保持INT_NZ状态；读取'0'和'3'同样保持INT_NZ状态；最后读取'0'时仍保持INT_NZ状态。扫描完成后，由于最终状态是INT_NZ，返回`<1, 12030>`。但如果学号包含4到9的数字，例如输入"45678"，读取'4'时就会从START状态转移到DEAD状态，返回`<3, 45678>`。

#include <vector>

测试2针对姓氏首字母加学号后2位。假设姓氏首字母为'a'，学号后2位为"13"，输入"a13"。分析过程：读取'a'，从START状态转移到ID状态；读取'1'，由于是有效数字字符，保持ID状态；读取'3'，同样保持ID状态。最终状态是ID，返回`<2, a13>`。如果姓氏首字母不在a到c范围，如输入"d13"，读取'd'时会从START状态转移到DEAD状态，返回`<3, d13>`。

// 词法分析核心函数#include <string>

测试3输入多个词含标识符和整数。输入"0 a2 23 ab 10"进行测试。程序通过stringstream将输入按空格分割成五个词素："0"、"a2"、"23"、"ab"、"10"。对每个词素分别调用lex_analyzer：

// 输入：待分析的词素（字符串）#include <sstream>

- 词素"0"：从START读'0'到INT_ZERO，是终态，返回`<1, 0>`

- 词素"a2"：从START读'a'到ID，读'2'保持ID，最终返回`<2, a2>`// 输出：pair<种别码, 属性值>

- 词素"23"：从START读'2'到INT_NZ，读'3'保持INT_NZ，返回`<1, 23>`

- 词素"ab"：从START读'a'到ID，读'b'保持ID，返回`<2, ab>`pair<int, string> lex_analyzer(const string &lexeme) {using namespace std;

- 词素"10"：从START读'1'到INT_NZ，读'0'保持INT_NZ，返回`<1, 10>`

    if (lexeme.empty()) return {ILLEGAL, lexeme};

这个测试验证了程序能够正确处理多个词，并且能够正确识别整数和标识符。

// 词法种别：1-整数，2-标识符，3-非法

测试4输入多个词含非法词。输入"4 d0 a4 c-1"进行测试。分析每个词素：

    // 定义DFA的五个状态enum TokenType { INTEGER = 1, IDENTIFIER = 2, ILLEGAL = 3 };

- 词素"4"：从START读'4'，不满足任何合法转移条件，转到DEAD，返回`<3, 4>`

- 词素"d0"：从START读'd'，不在a到c范围，转到DEAD，返回`<3, d0>`    enum State { START, INT_ZERO, INT_NZ, ID, DEAD };

- 词素"a4"：从START读'a'到ID，读'4'不在0到3范围，转到DEAD，返回`<3, a4>`

- 词素"c-1"：从START读'c'到ID，读'-'不是合法字符，转到DEAD，返回`<3, c-1>`    State s = START;static inline bool isDigit03(char c) { return c >= '0' && c <= '3'; }



这个测试验证了程序能够正确识别各种非法词，包括非法数字、非法字母、非法字符等。static inline bool isDigit123(char c) { return c >= '1' && c <= '3'; }



（此处应插入四组测试的运行截图，展示实际的输入输出过程）    // 逐字符扫描，进行状态转移static inline bool isLetterABC(char c) { return c >= 'a' && c <= 'c'; }



### 3.6 实验过程中遇到的问题及解决方案    for (size_t i = 0; i < lexeme.size(); ++i) {



问题1是关于前导零的处理。在最初的设计中，我没有将'0'作为特殊情况处理，导致像"01"、"012"这样的输入被错误地识别为合法整数。经过分析正规式 `0 | (1|2|3)(0|1|2|3)*`，我意识到这个正规式明确表示：要么是单独的0，要么是以非零数字开头的序列。因此，单独的'0'后面不能再有任何字符。解决方案是在DFA中设立一个专门的INT_ZERO状态。当在START状态读到'0'时，转移到INT_ZERO状态，这个状态虽然是终态（表示单独的'0'是合法的），但任何后续字符都会导致转移到DEAD状态。这样就正确实现了正规式的语义。在代码中体现为 `case INT_ZERO: s = DEAD; break;`，注释说明"单独的0后面不能有其他字符"。这个问题的严重程度为中等，因为它影响了词法分析的正确性，但通过仔细分析正规式的语义很容易发现和修复。        char c = lexeme[i];// 显式DFA实现



问题2是关于输入缓冲区处理。在编写测试函数时，我最初混用了`cin >>`和getline。这导致了一个经典的输入问题：使用`cin >>`读取数据后，输入缓冲区中会残留一个换行符，下一次调用getline会立即读到这个换行符而返回空字符串，导致无法正常读入用户输入。解决方案是统一使用getline函数读取所有输入。getline会读取整行内容并丢弃行尾的换行符，这样就避免了缓冲区残留问题。同时，配合stringstream使用提取运算符进行分词，既保证了输入的正确性，又实现了多词处理的功能。这个问题的严重程度为低，但对于用户体验影响较大，因为会导致程序跳过某些测试输入。        switch (s) {// 整数正规式：0 | (1|2|3)(0|1|2|3)*



问题3是关于空格和空字符串处理。在处理用户输入时，需要考虑各种边界情况。用户可能输入多余的空格，或者直接按回车输入空行。对于多余空格的问题，stringstream的提取运算符自动跳过前导和尾随的空白字符，并且以任何空白字符作为分隔符，所以天然地解决了这个问题。对于空字符串，在lex_analyzer函数的开头添加了检查：`if(lexeme.empty()) return {ILLEGAL, lexeme};` 这样确保空字符串被识别为非法词。这个问题的严重程度为低，但完善这些边界情况处理使程序更加健壮。            case START:// 标识符正规式：<字母>(<字母>|<数字字符>)*，其中 字母=a|b|c，数字字符=0|1|2|3



问题4是关于性能优化。在最初的实现中，即使已经确定某个词是非法的（状态转移到DEAD），循环仍然会继续处理剩余的字符。虽然这不影响正确性，但会造成不必要的计算。解决方案是在每次状态转移后立即检查当前状态是否为DEAD，如果是则直接返回。在代码中体现为 `if (s == DEAD) { return {ILLEGAL, lexeme}; }`。这个优化对于很长的非法词素可以显著减少不必要的字符处理。这个问题的严重程度为低，属于性能优化范畴。                if (c == '0') s = INT_ZERO;                       pair<int, string> lex_analyzer(const string &lexeme) {



### 3.7 程序的差异化设计                else if (isDigit123(c)) s = INT_NZ;                    if (lexeme.empty()) return {ILLEGAL, lexeme};



为了使程序区别于其他同学的实现，我在标准要求基础上进行了以下改进设计。改进A是代码结构优化，将字符判断逻辑封装成独立的内联函数（isDigit03、isDigit123、isLetterABC），而不是在主要逻辑中直接写判断表达式。这提高了代码的可读性和可维护性。如果将来需要扩展字符集，只需要修改这些辅助函数即可。改进B是错误处理的提前终止，在检测到非法字符时立即返回，而不是继续处理剩余字符。这不仅提高了性能，也体现了快速失败的设计理念。改进C是使用现代C++特性，使用强类型的枚举（TokenType和State）而不是宏定义或整数常量，使用范围for循环而不是传统的下标循环，使用auto关键字简化类型声明（在print_results中）。这些都是C++11和C++14引入的现代特性，使代码更加简洁和安全。改进D是输入输出性能优化，在main函数中调用 `ios::sync_with_stdio(false)` 和 `cin.tie(nullptr)` 优化IO性能，虽然对这个小程序影响不大，但体现了对性能的关注。                else if (isLetterABC(c)) s = ID;                  



潜在扩展方向包括：增加文件输入输出功能，从文件读取待分析的源程序，将结果输出到文件；使用二维状态转移表代替switch语句实现DFA，这样可以更方便地扩展到更复杂的文法；在程序启动时显示作者信息（学号、姓名等）；支持更大的字符集，如完整的字母表和十进制数字。                else s = DEAD;    enum State { START, INT_ZERO, INT_NZ, ID, DEAD };



## 四、实验总结                break;    State s = START;



通过本次实验，我深刻体会到了词法分析在编译器构造中的基础性作用，也掌握了从理论到实践的完整过程。在设计数据结构时，我充分考虑了问题的需求特点，选择枚举类型提高类型安全性，使用pair和vector简化数据组织，利用stringstream优雅地处理字符串分词。这些数据结构的合理选择大大简化了算法的实现难度。            case INT_ZERO:



实验中最大的难点是正确理解和实现正规式的语义，特别是对单独'0'的特殊处理。通过仔细分析正规式 `0 | (1|2|3)(0|1|2|3)*`，我意识到必须将"单独的0"作为特殊情况处理，设计独立的INT_ZERO状态。这个问题的解决让我认识到，在将形式化规约转换为程序实现时，必须准确把握每个细节的语义，不能想当然。另一个挑战是处理输入缓冲区的问题。混用不同的输入函数会导致难以调试的bug，通过统一使用getline并配合stringstream，我找到了优雅的解决方案。这个经验让我明白，在程序设计中保持一致性的重要性。                // 单独的0后面不能有其他字符    for (size_t i = 0; i < lexeme.size(); ++i) {



对程序的评价方面，我认为当前实现在正确性、可读性和效率上达到了较好的平衡。使用显式的DFA状态枚举使得状态转移逻辑清晰可见，便于理解和维护；提前终止非法词的处理提高了性能；使用STL容器和算法减少了手动内存管理的负担。但程序也有改进空间，例如可以将DFA的转移逻辑抽象为表驱动方式，这样扩展到更复杂的文法时会更加方便。                s = DEAD;        char c = lexeme[i];



影响效率的主要因素包括：字符串的复制操作，在process_input中每次提取词素都会创建新的string对象；状态转移判断的开销，虽然使用了switch语句，但仍然涉及多次条件判断。改进方法可以考虑使用string_view减少复制，或者使用查表法减少条件判断。                break;        switch (s) {



经过此次实验，我的主要收获包括：深入理解了DFA在词法分析中的应用，掌握了状态机的编程实现技巧，学会了如何将形式化的语言规约转换为可执行的程序，认识到了数据结构选择对程序设计的重要影响，以及体会到了充分测试和边界条件处理的必要性。这次实验不仅巩固了编译原理的理论知识，也提升了我的编程实践能力，为后续学习语法分析、语义分析等编译器其他组件打下了坚实基础。            case INT_NZ:            case START:


                if (isDigit03(c)) s = INT_NZ;                                    if (c == '0') s = INT_ZERO;                       // 只能是单独的0

                else s = DEAD;                else if (isDigit123(c)) s = INT_NZ;                // 非零整数前导位

                break;                else if (isLetterABC(c)) s = ID;                   // 标识符首字母

            case ID:                else s = DEAD;

                if (isLetterABC(c) || isDigit03(c)) s = ID;                       break;

                else s = DEAD;            case INT_ZERO:

                break;                // 单独的0后面出现任何字符都非法

            case DEAD:                s = DEAD;

                // 保持在DEAD状态                break;

                break;            case INT_NZ:

        }                if (isDigit03(c)) s = INT_NZ;                     // 后续均为0..3

                else s = DEAD;

        // 优化：一旦进入DEAD状态立即返回                break;

        if (s == DEAD) {            case ID:

            return {ILLEGAL, lexeme};                if (isLetterABC(c) || isDigit03(c)) s = ID;       // a..c 或 0..3

        }                else s = DEAD;

    }                break;

            case DEAD:

    // 根据终态判断词的类型                break;

    if (s == INT_ZERO || s == INT_NZ) return {INTEGER, lexeme};        }

    if (s == ID) return {IDENTIFIER, lexeme};

    return {ILLEGAL, lexeme};        if (s == DEAD) {

}            return {ILLEGAL, lexeme};

        }

// 处理一行输入（可能包含多个词，空格分隔）    }

// 输入：一行字符串

// 输出：所有词的分析结果    // 终态判断

vector<pair<int, string>> process_input(const string &line) {    if (s == INT_ZERO || s == INT_NZ) return {INTEGER, lexeme};

    vector<pair<int, string>> results;    if (s == ID) return {IDENTIFIER, lexeme};

    stringstream ss(line);    return {ILLEGAL, lexeme};

    string tok;}

    while (ss >> tok) {

        results.push_back(lex_analyzer(tok));// 处理输入（支持空格分隔多个词）

    }vector<pair<int, string>> process_input(const string &line) {

    return results;    vector<pair<int, string>> results;

}    stringstream ss(line);

    string tok;

// 打印分析结果    while (ss >> tok) {

void print_results(const vector<pair<int, string>> &res) {        results.push_back(lex_analyzer(tok));

    for (const auto &p : res) {    }

        cout << "<" << p.first << ", " << p.second << ">" << endl;    return results;

    }}

}

// 打印分析结果 <种别, 属性值>

// 测试函数：按要求完成四组测试void print_results(const vector<pair<int, string>> &res) {

void test() {    for (const auto &p : res) {

    // 测试1：学号后5位        cout << "<" << p.first << ", " << p.second << ">" << endl;

    cout << "=== 测试1：学号后5位 ===" << endl;    }

    cout << "请输入学号后5位：";}

    string input1; getline(cin, input1);

    print_results(process_input(input1));// 测试程序

void test() {

    // 测试2：姓氏首字母+学号后2位    // 测试1：学号后5位

    cout << "\n=== 测试2：姓氏首字母+学号后2位 ===" << endl;    cout << "=== 测试1：学号后5位 ===" << endl;

    cout << "请输入姓氏首字母+学号后2位：";    cout << "请输入学号后5位：";

    string input2; getline(cin, input2);    string input1; getline(cin, input1);

    print_results(process_input(input2));    print_results(process_input(input1));



    // 测试3：多个词（含标识符和整数）    // 测试2：姓氏首字母+学号后2位

    cout << "\n=== 测试3：多个词（含标识符和整数） ===" << endl;    cout << "\n=== 测试2：姓氏首字母+学号后2位 ===" << endl;

    cout << "请输入多个词（空格分隔）：";    cout << "请输入姓氏首字母+学号后2位：";

    string input3; getline(cin, input3);    string input2; getline(cin, input2);

    print_results(process_input(input3));    print_results(process_input(input2));



    // 测试4：多个词（含非法词）    // 测试3：多个词（含标识符和整数）

    cout << "\n=== 测试4：多个词（含非法词） ===" << endl;    cout << "\n=== 测试3：多个词（含标识符和整数） ===" << endl;

    cout << "请输入多个词（空格分隔，含非法词）：";    cout << "请输入多个词（空格分隔）：";

    string input4; getline(cin, input4);    string input3; getline(cin, input3);

    print_results(process_input(input4));    print_results(process_input(input3));

}

    // 测试4：多个词（含非法词）

int main() {    cout << "\n=== 测试4：多个词（含非法词） ===" << endl;

    ios::sync_with_stdio(false);    cout << "请输入多个词（空格分隔，含非法词）：";

    cin.tie(nullptr);    string input4; getline(cin, input4);

    test();    print_results(process_input(input4));

    return 0;}

}

```int main() {

    ios::sync_with_stdio(false);

### 3.5 程序测试过程    cin.tie(nullptr);

    test();

测试按照实验要求进行了四组，每组测试都经过精心设计以验证程序的不同功能。以下是测试的详细过程和结果分析。    return 0;

}

**测试1：学号后5位**```

假设输入学号后5位为"12030"，由于包含数字4到9范围之外的字符，程序会将其识别为非法。如果输入"12030"，分析过程如下：程序首先读取字符'1'，从START状态转移到INT_NZ状态；继续读取'2'，由于'2'在0到3范围内，保持INT_NZ状态；读取'0'和'3'同样保持INT_NZ状态；最后读取'0'时仍保持INT_NZ状态。扫描完成后，由于最终状态是INT_NZ，返回 `<1, 12030>`。但如果学号包含'4'到'9'的数字，则会被识别为非法。

## 附录二：可选改进与拓展

**测试2：姓氏首字母+学号后2位**- 文件 I/O：从 input.txt 读取、写入 output.txt；

假设姓氏首字母为'a'，学号后2位为"13"，输入"a13"。分析过程：读取'a'，从START状态转移到ID状态；读取'1'，由于是有效数字字符，保持ID状态；读取'3'，同样保持ID状态。最终状态是ID，返回 `<2, a13>`。如果姓氏首字母不在a到c范围，如'd'，则会被识别为非法。- 表驱动 DFA：用二维数组维护“状态×字符类”的转移；

- UI：打印学号/姓名，或给输出加水印；

**测试3：多个词（含标识符和整数）**- 文法扩展：增加关键字、分隔符、运算符等。

输入"0 a2 23 ab 10"进行测试。程序通过stringstream将输入按空格分割成五个词素："0"、"a2"、"23"、"ab"、"10"。对每个词素分别调用lex_analyzer：

- "0"：从START读'0'到INT_ZERO，是终态，返回 `<1, 0>`## 附录三：C 语言版本（可选）

- "a2"：从START读'a'到ID，读'2'保持ID，最终返回 `<2, a2>`下面给出一个等价的 C 语言实现（用于满足“建议采用 C 语言”的扩展需求）。

- "23"：从START读'2'到INT_NZ，读'3'保持INT_NZ，返回 `<1, 23>`

- "ab"：从START读'a'到ID，读'b'保持ID，返回 `<2, ab>````c

- "10"：从START读'1'到INT_NZ，读'0'保持INT_NZ，返回 `<1, 10>`#include <stdio.h>

#include <string.h>

这个测试验证了程序能够正确处理多个词，并且能够正确识别整数和标识符。#include <ctype.h>



**测试4：多个词（含非法词）**enum { INTEGER = 1, IDENTIFIER = 2, ILLEGAL = 3 };

输入"4 d0 a4 c-1"进行测试。分析每个词素：

- "4"：从START读'4'，不满足任何合法转移条件，转到DEAD，返回 `<3, 4>`static int is_digit03(char c){ return c >= '0' && c <= '3'; }

- "d0"：从START读'd'，不在a到c范围，转到DEAD，返回 `<3, d0>`static int is_digit123(char c){ return c >= '1' && c <= '3'; }

- "a4"：从START读'a'到ID，读'4'不在0到3范围，转到DEAD，返回 `<3, a4>`static int is_letterabc(char c){ return c >= 'a' && c <= 'c'; }

- "c-1"：从START读'c'到ID，读'-'不是合法字符，转到DEAD，返回 `<3, c-1>`

int lex_analyzer_c(const char* s){

这个测试验证了程序能够正确识别各种非法词，包括非法数字、非法字母、非法字符等。    if(!s || !*s) return ILLEGAL;

    enum { START, INT_ZERO, INT_NZ, ID, DEAD } st = START;

**（此处应插入四组测试的运行截图）**    for(size_t i=0; s[i]; ++i){

        char c = s[i];

### 3.6 实验过程中遇到的问题及解决方案        switch(st){

            case START:

**问题1：前导零的处理**                if(c=='0') st = INT_ZERO;

在最初的设计中，我没有将"0"作为特殊情况处理，导致像"01"、"012"这样的输入被错误地识别为合法整数。经过分析正规式 `0 | (1|2|3)(0|1|2|3)*`，我意识到这个正规式明确表示：要么是单独的0，要么是以非零数字开头的序列。因此，单独的0后面不能再有任何字符。                else if(is_digit123(c)) st = INT_NZ;

                else if(is_letterabc(c)) st = ID;

解决方案是在DFA中设立一个专门的INT_ZERO状态。当在START状态读到'0'时，转移到INT_ZERO状态，这个状态虽然是终态（表示单独的0是合法的），但任何后续字符都会导致转移到DEAD状态。这样就正确实现了正规式的语义。在代码中体现为：                else st = DEAD;

```cpp                break;

case INT_ZERO:            case INT_ZERO:

    s = DEAD;  // 单独的0后面不能有其他字符                st = DEAD; // 单独0后续非法

    break;                break;

```            case INT_NZ:

                if(is_digit03(c)) st = INT_NZ; else st = DEAD;

这个问题的严重程度为中等，因为它影响了词法分析的正确性，但通过仔细分析正规式的语义很容易发现和修复。                break;

            case ID:

**问题2：输入缓冲区处理**                if(is_letterabc(c) || is_digit03(c)) st = ID; else st = DEAD;

在编写测试函数时，我最初混用了 `cin >>` 和 `getline`。这导致了一个经典的输入问题：使用 `cin >>` 读取数据后，输入缓冲区中会残留一个换行符，下一次调用 `getline` 会立即读到这个换行符而返回空字符串，导致无法正常读入用户输入。                break;

            case DEAD:

解决方案是统一使用 `getline` 函数读取所有输入。`getline` 会读取整行内容并丢弃行尾的换行符，这样就避免了缓冲区残留问题。同时，配合stringstream使用提取运算符进行分词，既保证了输入的正确性，又实现了多词处理的功能。这个问题的严重程度为低，但对于用户体验影响较大，因为会导致程序跳过某些测试输入。                break;

        }

**问题3：空格和空字符串处理**        if(st==DEAD) return ILLEGAL;

在处理用户输入时，需要考虑各种边界情况。用户可能输入多余的空格，或者直接按回车输入空行。对于多余空格的问题，stringstream的提取运算符 `>>` 自动跳过前导和尾随的空白字符，并且以任何空白字符作为分隔符，所以天然地解决了这个问题。对于空字符串，在lex_analyzer函数的开头添加了检查：    }

```cpp    if(st==INT_ZERO || st==INT_NZ) return INTEGER;

if (lexeme.empty()) return {ILLEGAL, lexeme};    if(st==ID) return IDENTIFIER;

```    return ILLEGAL;

这样确保空字符串被识别为非法词。这个问题的严重程度为低，但完善这些边界情况处理使程序更加健壮。}



**问题4：性能优化**// 将一行按空格分词并输出 <种别, 属性>

在最初的实现中，即使已经确定某个词是非法的（状态转移到DEAD），循环仍然会继续处理剩余的字符。虽然这不影响正确性，但会造成不必要的计算。void process_line(const char* line){

    char buf[1024];

解决方案是在每次状态转移后立即检查当前状态是否为DEAD，如果是则直接返回：    strncpy(buf, line, sizeof(buf));

```cpp    buf[sizeof(buf)-1] = '\0';

if (s == DEAD) {    const char* sep = " \t\r\n";

    return {ILLEGAL, lexeme};    char* tok = strtok(buf, sep);

}    while(tok){

```        int t = lex_analyzer_c(tok);

这个优化对于很长的非法词素可以显著减少不必要的字符处理。这个问题的严重程度为低，属于性能优化范畴。        printf("<%d, %s>\n", t, tok);

        tok = strtok(NULL, sep);

### 3.7 程序的差异化设计    }

}

为了使程序区别于其他同学的实现，我在标准要求基础上进行了以下改进设计：

int main(){

**改进A：代码结构优化**    char line[1024];

将字符判断逻辑封装成独立的内联函数（isDigit03、isDigit123、isLetterABC），而不是在主要逻辑中直接写判断表达式。这提高了代码的可读性和可维护性。如果将来需要扩展字符集，只需要修改这些辅助函数即可。    // 测试1

    printf("=== 测试1：学号后5位 ===\n");

**改进B：错误处理的提前终止**    printf("请输入学号后5位：");

在检测到非法字符时立即返回，而不是继续处理剩余字符。这不仅提高了性能，也体现了"快速失败"的设计理念。    if(fgets(line, sizeof(line), stdin)) process_line(line);

    // 测试2

**改进C：使用现代C++特性**    printf("\n=== 测试2：姓氏首字母+学号后2位 ===\n");

使用强类型的枚举（TokenType和State）而不是宏定义或整数常量，使用范围for循环而不是传统的下标循环，使用auto关键字简化类型声明（在print_results中）。这些都是C++11/14引入的现代特性，使代码更加简洁和安全。    printf("请输入姓氏首字母+学号后2位：");

    if(fgets(line, sizeof(line), stdin)) process_line(line);

**改进D：输入输出性能优化**    // 测试3

在main函数中调用 `ios::sync_with_stdio(false)` 和 `cin.tie(nullptr)` 优化IO性能，虽然对这个小程序影响不大，但体现了对性能的关注。    printf("\n=== 测试3：多个词（含标识符和整数） ===\n");

    printf("请输入多个词（空格分隔）：");

**潜在扩展方向**    if(fgets(line, sizeof(line), stdin)) process_line(line);

如果进一步扩展，可以考虑：1）增加文件输入输出功能，从文件读取待分析的源程序，将结果输出到文件；2）使用二维状态转移表代替switch语句实现DFA，这样可以更方便地扩展到更复杂的文法；3）在程序启动时显示作者信息（学号、姓名等）；4）支持更大的字符集，如完整的字母表和十进制数字。    // 测试4

    printf("\n=== 测试4：多个词（含非法词） ===\n");

## 四、实验总结    printf("请输入多个词（空格分隔，含非法词）：");

    if(fgets(line, sizeof(line), stdin)) process_line(line);

通过本次实验，我深刻体会到了词法分析在编译器构造中的基础性作用，也掌握了从理论到实践的完整过程。在设计数据结构时，我充分考虑了问题的需求特点，选择枚举类型提高类型安全性，使用pair和vector简化数据组织，利用stringstream优雅地处理字符串分词。这些数据结构的合理选择大大简化了算法的实现难度。    return 0;

}

实验中最大的难点是正确理解和实现正规式的语义，特别是对单独0的特殊处理。通过仔细分析正规式 `0 | (1|2|3)(0|1|2|3)*`，我意识到必须将"单独的0"作为特殊情况处理，设计独立的INT_ZERO状态。这个问题的解决让我认识到，在将形式化规约转换为程序实现时，必须准确把握每个细节的语义，不能想当然。```



另一个挑战是处理输入缓冲区的问题。混用不同的输入函数会导致难以调试的bug，通过统一使用getline并配合stringstream，我找到了优雅的解决方案。这个经验让我明白，在程序设计中保持一致性的重要性。（说明：C 版本仅作为附加材料，仓库默认构建的是 C++ 版本 `main.cpp`。）



对程序的评价方面，我认为当前实现在正确性、可读性和效率上达到了较好的平衡。使用显式的DFA状态枚举使得状态转移逻辑清晰可见，便于理解和维护；提前终止非法词的处理提高了性能；使用STL容器和算法减少了手动内存管理的负担。但程序也有改进空间，例如可以将DFA的转移逻辑抽象为表驱动方式，这样扩展到更复杂的文法时会更加方便。—— 完 ——


影响效率的主要因素包括：字符串的复制操作，在process_input中每次提取词素都会创建新的string对象；状态转移判断的开销，虽然使用了switch语句，但仍然涉及多次条件判断。改进方法可以考虑使用字符串视图（string_view）减少复制，或者使用查表法减少条件判断。

经过此次实验，我的主要收获包括：深入理解了DFA在词法分析中的应用，掌握了状态机的编程实现技巧，学会了如何将形式化的语言规约转换为可执行的程序，认识到了数据结构选择对程序设计的重要影响，以及体会到了充分测试和边界条件处理的必要性。这次实验不仅巩固了编译原理的理论知识，也提升了我的编程实践能力，为后续学习语法分析、语义分析等编译器其他组件打下了坚实基础。
